# Laravel/local application environment (if this directory becomes the app root)
.env
.env.*
!.env.example

# Docker development credentials and local data
docker/env/*.env
!docker/env/*.env.example

docker/secrets/*
!docker/secrets/.gitkeep

.local/
docker/backups/
