bind 0.0.0.0
protected-mode yes
port 6379

dir /data
appendonly yes
appendfsync everysec
aof-use-rdb-preamble yes

# Queued jobs and locks must not be evicted under memory pressure.
maxmemory-policy noeviction
stop-writes-on-bgsave-error yes

save 900 1
save 300 10
save 60 10000

loglevel notice
